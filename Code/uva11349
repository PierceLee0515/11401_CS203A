#include <iostream>
using namespace std;
int main(){
    int t,n;
    char ch;
    cin >> t;
    for (int c = 1; c <= t; c++) {
        cin >> ch >> ch >> n;
        long long a[n][n];
        bool flag = true;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                cin >> a[i][j];
                if (a[i][j] < 0) {
                    flag = false;
                }
            }
        }
        if (!flag) {
            cout << "Test #" << c << ": " << "Non-symmetric."<<endl;
            continue;
        }

        for (int i = 0; i <= n / 2; i++) {
            for (int j = 0; j < n - i; j++) {
                if (a[i][j] != a[n - 1 - i][n - 1 - j]) {
                    flag = false;
                    break;
                }
            }
        }
        if (flag) cout << "Test #" << c << ": " << "Symmetric."<<endl;
        else cout << "Test #" << c << ": " << "Non-symmetric."<<endl;
    }
    return 0;
}


